{
  "name": "quiz-generator-ai-mcp",
  "description": "> **By [MEOK AI Labs](https://meok.ai)** \u2014 Quiz generation and assessment by MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "generate_quiz",
      "description": "Generate a quiz from provided content text.",
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string"
          },
          "num_questions": {
            "type": "integer"
          },
          "question_type": {
            "type": "string"
          }
        },
        "required": [
          "content"
        ]
      }
    },
    {
      "name": "validate_answers",
      "description": "Validate submitted answers against correct answers and return scoring.",
      "parameters": {
        "type": "object",
        "properties": {
          "questions": {
            "type": "array"
          },
          "answers": {
            "type": "object"
          }
        },
        "required": [
          "questions",
          "answers"
        ]
      }
    },
    {
      "name": "generate_flashcards",
      "description": "Generate flashcards (front/back pairs) from content for study purposes.",
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string"
          },
          "num_cards": {
            "type": "integer"
          }
        },
        "required": [
          "content"
        ]
      }
    },
    {
      "name": "assess_difficulty",
      "description": "Assess the difficulty level of content for quiz/study purposes.",
      "parameters": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string"
          },
          "target_audience": {
            "type": "string"
          }
        },
        "required": [
          "content"
        ]
      }
    }
  ]
}